version: 2

sources:
  - name: practicefusion_raw
    database: "{% if var('ENV', 'dev') == 'dev' %}dev{% elif var('ENV', 'prod') == 'prod' %}main{% else %}{{ var('ENV') }}{% endif %}"
    schema: practicefusion
    tables:
      - name: condition_diagnoses_raw
      - name: condition_problem_list_raw
      - name: encounter_raw
      - name: lab_result_raw
      - name: medication_raw
      - name: patient_raw
      - name: procedure_raw
      - name: practitioner_raw
      - name: vitals_raw
